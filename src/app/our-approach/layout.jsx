import React from "react";

export const metadata = {
  title: "Our Approach",
  description: "",
};

const ApproachLayout = ({ children }) => {
  return <>{children}</>;
};

export default ApproachLayout;
